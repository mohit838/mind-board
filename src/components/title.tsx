const Title = () => {
  return (
    <div className="mx-auto text-center mt-10">
      <h1 className="text-4xl">Creating basic Drag & Drop 👆 </h1>
      <span>( without external libraries )</span>
    </div>
  );
};

export default Title;
